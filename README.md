- 'npm start' command to run development build of the project.
    "start": "webpack-dev-server" => run webserver
    "start": "webpack" => run dev build

- 'npm build' command to run production build of the project